<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.InventoryPro" parent="android:Theme.Material.Light.NoActionBar" />

    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">#00121D</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_app_logo</item>

        <!-- This MUST be transparent to hide the circle -->
        <item name="windowSplashScreenIconBackgroundColor">@android:color/transparent</item>

        <item name="postSplashScreenTheme">@style/Theme.InventoryPro</item>
    </style>

</resources>